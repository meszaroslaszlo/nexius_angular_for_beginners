import { EditorInputFieldDirective } from './editor-input-field.directive';

describe('EditorInputFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorInputFieldDirective();
    expect(directive).toBeTruthy();
  });
});
